# Imagen Docker: solo lo que usa el bot (python:3.11-slim).
# Desarrollo local: sigue siendo `pip install -r requirements.txt` si quieres el entorno completo.
python-telegram-bot[job-queue]==22.7
python-dotenv==1.2.1
requests==2.32.5
pydantic-settings==2.13.1
SQLAlchemy==2.0.46
psycopg[binary]==3.3.2
alembic==1.18.4
# OCR (OCRService): mismas versiones que requirements-web.txt
pillow==12.1.0
pytesseract==0.3.13
pymupdf==1.25.5
