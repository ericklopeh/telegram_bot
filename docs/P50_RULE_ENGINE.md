# P50 — Motor de reglas dinámicas

- Modelo: `dynamic_rules` (`conditions_json`, `actions_json`)
- Servicio: `app/services/rule_engine.py`
- Panel: `/admin/rules` (seed reglas demo sección 21 / préstamos)
- Triggers: `sale_validation`, extensible sin redeploy.
