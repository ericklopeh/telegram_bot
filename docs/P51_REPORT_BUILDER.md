# P51 — Constructor de reportes

- Modelo: `SavedReport`
- Servicio: `app/services/report_builder_service.py`
- Rutas: `/reports/builder`, `/reports/saved`, `POST /reports/run` (CSV)
- Fuentes: `cases`, `sales` con columnas y filtros configurables.
